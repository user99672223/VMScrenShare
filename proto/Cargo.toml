[package]
name = "proto"
description = "vmdesk shared wire protocol: control/mouse messages, key codes, signalling types and SDP helpers"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
